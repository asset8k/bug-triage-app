# Backend API + ML engines (FastAPI, uvicorn, and dependencies for src.engines)
# Python 3.10+

fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.0.0

# ML engines (baseline, ollama, finetuned)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
joblib>=1.3.0
xgboost>=2.0.0
requests>=2.28.0

# Document parsing (upload / parse-document)
python-multipart>=0.0.6
pypdf>=4.0.0
python-docx>=1.0.0

# Optional: for fine-tuned GGUF engine (uncomment if using llama_finetuned.gguf or qwen_finetuned.gguf)
# llama-cpp-python>=0.2.0
